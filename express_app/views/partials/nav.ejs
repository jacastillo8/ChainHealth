<nav class="navbar navbar-inverse navbar-expand-lg navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-mysignals">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span> 
                <span class="icon-bar"></span> 
                <span class="icon-bar"></span> 
            </button>
            <a href="/" class="navbar-brand"><span><i class="fas fa-laptop-medical"></i> Home</span></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-nav-mysignals">
            <ul class="nav navbar-nav mr-auto">
                <% if (user) { %>
                    <div class="dropdown">
                        <button class="dropbtn"><i class="fas fa-wrench"></i> Services</button>
                        <div class="dropdown-content">
                            <% if (service !== null) { %>
                                <a href="<%= service %>"><i class="fas fa-file-medical-alt"></i> OpenEMR</a>
                            <% } %>
                            <% if (role.indexOf('auditor') > -1) { %>
                                <a href="/blockchain"><i class="fas fa-link"></i> Chainmed</a>
                            <% } %>
                            <% if (role.indexOf('healthcare') > -1) { %>
                                <a href="/devices"><i class="fas fa-mobile-alt"></i> Device Manager</a>
                            <% } %>
                        </div>
                    </div>
                <% } %> 
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if (user) { %>
                    <% if (role.indexOf('healthcare') > -1) { %>
                        <li><a href="/patient"><i class="fas fa-user-injured"></i> Patients</a></li>
                        <li><a href="/patient/new"><i class="fas fa-notes-medical"></i> New Patient</a></li>
                    <% } %>
                    <li><a href="/users/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                <% } else { %>
                    <li><a href="/users/login"><i class="fas fa-sign-in-alt"></i> Login</a></li>
                    <% if (no_health_users.length !== 0) { %>
                        <li><a href="/users/register"><i class="fas fa-user-plus"></i> Sign-Up</a></li>
                    <% } %>
                <% } %> 
            </ul>
        </div>
    </div>
</nav> 